# Server
PORT=4000
NODE_ENV=development

# Database — use a dedicated PostgreSQL user (see README: CREATE USER ... CREATE DATABASE ...)
DATABASE_URL=postgresql://student_dashboard_app:your_secure_password@localhost:5432/student_dashboard

# JWT (use a long random string in production: openssl rand -hex 32)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Cookie (set to true when using HTTPS in production)
COOKIE_SECURE=false
